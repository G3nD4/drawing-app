import 'package:drawing_application/features/get_it/get_it.dart';
import 'package:injectable/injectable.dart';
// import 'package:path_provider/path_provider.dart';

@module
abstract class Module {
  // @preResolve
  // Future<FileDirectory> get logFilePath async {
  //   return FileDirectory(
  //       '${(await getApplicationCacheDirectory()).path}/log.txt');

    // if (environment == Environment.test) {
    //   return FileDirectory('${(await getTemporaryDirectory()).path}/log.txt');
    // }
    // if (environment == Environment.dev) {
    //   return FileDirectory('${(await getApplicationCacheDirectory()).path}/log.txt');
    // }
    // return FileDirectory('');
  // }
}

// @Singleton
// class FileDirectory {
//   FileDirectory(this.name);

//   final String name;
// }
